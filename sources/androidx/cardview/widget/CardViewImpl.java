package androidx.cardview.widget;

import android.content.Context;
import android.content.res.ColorStateList;
import androidx.annotation.Nullable;

interface CardViewImpl {
    void a(CardViewDelegate cardViewDelegate, Context context, ColorStateList colorStateList, float f2, float f3, float f4);

    void b(CardViewDelegate cardViewDelegate, float f2);

    float c(CardViewDelegate cardViewDelegate);

    float d(CardViewDelegate cardViewDelegate);

    void e(CardViewDelegate cardViewDelegate);

    void f(CardViewDelegate cardViewDelegate, float f2);

    float g(CardViewDelegate cardViewDelegate);

    ColorStateList h(CardViewDelegate cardViewDelegate);

    void i(CardViewDelegate cardViewDelegate);

    void j();

    float k(CardViewDelegate cardViewDelegate);

    float l(CardViewDelegate cardViewDelegate);

    void m(CardViewDelegate cardViewDelegate);

    void n(CardViewDelegate cardViewDelegate, @Nullable ColorStateList colorStateList);

    void o(CardViewDelegate cardViewDelegate, float f2);
}
